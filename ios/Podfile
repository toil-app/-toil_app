# # Resolve react_native_pods.rb with node to allow for hoisting
# require Pod::Executable.execute_command('node', ['-p',
#   'require.resolve(
#     "react-native/scripts/react_native_pods.rb",
#     {paths: [process.argv[1]]},
#   )', __dir__]).strip

# platform :ios, min_ios_version_supported


# # Note: modular headers were previously enabled globally which can cause
# # issues with some Objective-C/Swift bridging and Xcode module caches
# # (e.g. 'Could not build module UIKit'). Prefer enabling modular headers
# # only for the specific pods that require them instead of globally.
# prepare_react_native_project!

# linkage = ENV['USE_FRAMEWORKS']
# if linkage != nil
#   Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
#   use_frameworks! :linkage => linkage.to_sym
# end

# target 'ToilApp' do
#   config = use_native_modules!
#    $RNFirebaseAsStaticFramework = true
#   # Ensure modular headers for specific Google/Firebase pods required by
#   # Swift-based Firebase targets. These entries are conservative and avoid
#   # enabling `use_modular_headers!` globally which can cause UIKit module
#   # build errors in some Xcode/toolchain combinations.
#   # pod 'GoogleUtilities', :modular_headers => true
# #  pod 'FirebaseCoreInternal', :modular_headers => true
# #  pod 'FirebaseCore', :modular_headers => true
# #  pod 'FirebaseRemoteConfig', :modular_headers => true
# #  pod 'FirebaseABTesting', :modular_headers => true
# #  pod 'FirebaseInstallations', :modular_headers => true
# #  pod 'FirebaseFirestore', :modular_headers => true
# #  pod 'FirebaseFirestoreInternal', :modular_headers => true
#   pod 'RNFS', :path => '../node_modules/react-native-fs'

#   use_react_native!(
#     :path => config[:reactNativePath],
#     # An absolute path to your application root.
#     :app_path => "#{Pod::Config.instance.installation_root}/.."
#   )

#   post_install do |installer|
#     react_native_post_install(
#       installer,
#       config[:reactNativePath],
#       :mac_catalyst_enabled => false,
#       # :ccache_enabled => true
#     )
#   end
# end
# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, min_ios_version_supported

# REQUIRED for Firebase Swift pods
use_modular_headers!
use_frameworks! :linkage => :static

# REQUIRED for React Native Firebase
$RNFirebaseAsStaticFramework = true

prepare_react_native_project!

target 'ToilApp' do
  config = use_native_modules!

  pod 'RNFS', :path => '../node_modules/react-native-fs'

  use_react_native!(
    :path => config[:reactNativePath],
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  post_install do |installer|
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )
  end
end
